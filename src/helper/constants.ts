export const preservedRenderRoutes: Array<string> = ['/qna'];

// app routes
export enum AppRoutes {
    HOME = '/',
    QNA = '/qna',
    DOCS = '/docs',
    GitHub = 'https://github.com/RandomsTS'
}
